import { Database } from '../database';
import { CollectionToImport } from '../common';
export declare class DataImporter {
    db: Database;
    constructor(db: Database);
    import(collections: CollectionToImport[]): Promise<void>;
    importCollection(collection: CollectionToImport): Promise<void>;
}
