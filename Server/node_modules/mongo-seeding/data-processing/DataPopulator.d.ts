import { CollectionToImport } from '../common';
export declare class DataPopulator {
    supportedExtensions: string[];
    constructor(supportedExtensions: string[]);
    populate(inputDirectory: string): CollectionToImport[];
    readCollections(directories: string[], inputDirectory: string): CollectionToImport[];
    readCollection(path: string, directoryName: string): {
        name: string;
        documents: any[];
    } | null;
    populateDocumentsContent(collectionPath: string): any[] | undefined;
    getCollectionName(directoryName: string): string;
}
