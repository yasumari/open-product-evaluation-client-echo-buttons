import { Db } from 'mongodb';
export declare class Database {
    db: Db;
    constructor(db: Db);
    insertDocumentsIntoCollection(documentsToInsert: any[], collectionName: string): Promise<void>;
    drop(): Promise<void>;
}
